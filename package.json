{
  "name": "hego-digital",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "echo 'Build complete - static HTML/CSS/JS project, no build step required.' && exit 0"
  }
}
